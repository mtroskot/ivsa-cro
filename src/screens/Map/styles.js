import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  mapContainer: {
    flex: 0.4
  },
  pickerLocationView: {
    flex: 0.6
  }
});

export default styles;
